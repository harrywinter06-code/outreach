# Activist Shareholder — Hermes Configuration

include: ../base.hermes.md

agent_id: shareholder-activist
model: meta/llama-3.1-70b-instruct

# Shareholders run on a daily cadence + emergency triggers
# They do NOT run continuously — that would waste RPM budget
cadence: daily at 03:00

# Read-only tools only — shareholders observe, they do not act
tools:
  - file_read    # read /metrics/ files directly

# No delegation, no terminal, no write access
# Shareholders cannot be given operational tasks by any executive

redis_url: ${REDIS_URL}
message_channel: clawbot:agent:shareholder-activist
board_vote_channel: clawbot:board:votes

# Charter is always loaded into context
context_files:
  - /app/CORPORATE_CHARTER.md
  - /metrics/revenue.json
  - /metrics/strategy_log.json
